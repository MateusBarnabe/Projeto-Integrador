import { EmConstrucao } from '@/components/EmConstrucao';

export function FormulariosPage() {
  return (
    <div className="p-5">
      <EmConstrucao descricao="Construtor de campos com parte fixa (nome, e-mail, telefone e consentimento) e parte configurável, pré-visualização e publicação de versões (norteador 9.5)." />
    </div>
  );
}
