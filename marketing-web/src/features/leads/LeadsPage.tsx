import { EmConstrucao } from '@/components/EmConstrucao';

export function LeadsPage() {
  return (
    <div className="p-5">
      <EmConstrucao descricao="Lista com filtros por etapa, faixa, origem, campanha e vendedor; detalhe do lead em modal com respostas, pontuação, consentimento e timeline (norteador 9.2)." />
    </div>
  );
}
